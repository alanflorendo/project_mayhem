<% @hypo.comments.each do |c| %>
  <% c_votes = c.vote_count == nil ? 0 : c.vote_count %>
  <%= "#{c.user.username}: (#{c_votes})" %><br>
  <% time = c.created_at.in_time_zone("Eastern Time (US & Canada)") %>
  <%= time.strftime("%m/%d/%Y %I:%M%p") %><br>
  <%= c.text %><br><br>
<% end %>
